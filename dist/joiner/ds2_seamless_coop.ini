# DS2 Seamless Co-op — JOINER Configuration
# Drop all files in: Dark Souls II Scholar of the First Sin/Game/
#
# Set server_ip to the host's IP address (LAN, Hamachi, or public).

enabled=true
debug_logging=false
max_players=6
port=27015

# Custom server (runs on host's machine)
use_custom_server=true
server_ip=25.35.223.224
server_port=50031

# Gameplay
allow_invasions=false
sync_bonfires=true
sync_items=false
sync_enemies=false
